[pytest]
python_files = test_*
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning

markers =
    smoke: All critical smoke tests
    comparison: marks tests as cross-framework comparison (skipped unless results JSON files exist)
    slow: marks tests as slow-running (use -m "not slow" to skip)
